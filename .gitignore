.tmp/
legacy/tools/node_modules/

# proyecto tscircuit (legacy) — build artefacts and local deps
legacy/tscircuit/.tscircuit/
legacy/tscircuit/dist/
legacy/tscircuit/node_modules/
# package-lock.json is tracked for reproducible installs (pinned deps)

# KiCad local-only files (backups, per-user prefs, footprint cache)
hardware/kicad-v3/*-backups/
hardware/kicad-v3/*.kicad_prl
hardware/kicad-v3/fp-info-cache

# firmware local files
firmware/esphome/secrets.yaml
firmware/esphome/.esphome/

# python bytecode
__pycache__/
*.pyc
